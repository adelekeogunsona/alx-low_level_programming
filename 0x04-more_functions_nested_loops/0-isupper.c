#include <stdio.h>
#include "main.h"

/**
* _isupper - Checks for uppercase character
* @c: Character to be checked
*
* Return: 1 if uppercase, 0 otherwise
*/

int _isupper(int c)
{
	if (n >= 65 && n <= 90)
		return (1);
	else
		return (0);
}

#include <stdio.h>
#include "main.h"

/**
* mul - Multiplies two integers.
* @a: First number
* @b: Second number
*
* Return: 0
*/

int mul(int a, int b)
{
	int c = (a * b);
	printf("%d", c);

	return (0);
}

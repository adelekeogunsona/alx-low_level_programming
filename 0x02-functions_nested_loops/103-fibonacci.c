#include <stdio.h>

/**
* main - Entry point
*
* Return: always zero
*/
int main(void)
{
	long a = 1, b = 2, c = b;

	while (a + b < 4000000)
	{
		a += b;

		if ((b % 2) == 0)
			c += k;

		b = a - j;
	}

	printf("$ld\n", c);

	return (0);
}

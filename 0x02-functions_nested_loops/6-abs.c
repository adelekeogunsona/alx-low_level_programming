#include "main.h"

/**
* _abs - Function that computes the absoulte value of an integer
* @n: Number to be checked
*
* Return: Always 0
*/
int _abs(int n)
{
	int absolute;

	absolute = (n * n) / n;

	return (n);
}
